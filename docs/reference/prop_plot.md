# Create propensity score distribution plot

Creates a density plot showing the distribution of propensity scores for
treatment and control groups

## Usage

``` r
prop_plot(e.hat, W)
```

## Arguments

- e.hat:

  Vector of estimated propensity scores

- W:

  Vector of treatment assignments

## Value

A ggplot object
